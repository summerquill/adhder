# ADHDer MVP 产品设计文档

## 产品方向

ADHDer 是一款帮助 ADHD 用户降低生活管理压力的轻量工具。MVP 阶段不追求完整的效率系统，而是优先解决一个核心问题：用户脑子里有很多事情，但很难整理、选择并开始。

第一版产品的重点是帮助用户完成从“想到一件事”到“开始做一个很小的动作”的过程。

最终产品形态优先考虑移动 App 或微信小程序。Web 版本采用移动端优先设计，交互结构尽量贴近 App，便于后续迁移到 App 或小程序。

## 产品目标

帮助 ADHDer 把脑子里的混乱事项快速收集起来，选出今天最重要的少数任务，并把它们转化成可以立刻开始的小动作。

## 核心用户问题

- 脑子里同时装着很多任务，容易混乱和过载。
- 待办事项太多时，不知道今天应该先做什么。
- 任务描述太大或太模糊，导致迟迟无法开始。
- 已经知道要做什么，但启动成本很高。
- 任务状态不是简单的完成或未完成，中间经常会暂停、转移或搁置。

## MVP 功能范围

### 1. 快速 Inbox

用户可以随手记录脑子里的任务、想法和待办事项，不需要立即分类、排序或安排时间。

目标是降低输入压力，让用户先把事情从脑子里倒出来。

### 2. 今日 3 件事

“3 件事”作为建议和注意力提示，而不是强制限制。

建议用户先选择 1～3 件今天推进；如果用户想继续添加，也可以根据自己的状态加入更多任务。

默认展示建议数量的作用是减少决策压力，但产品不阻止用户管理自己的今日重点。

今日列表和快速 Inbox 使用同一套排序：按状态分组展示，顺序为正在执行 → 未开始 → 暂时放下 → 完成；同一组内按任务添加时间从早到晚排列。

### 3. 任务拆成下一步

系统辅助用户把一个模糊任务拆成一个更小、更容易开始的下一步动作。

用户可以接受、编辑或重新生成系统建议。系统负责降低拆解成本，用户保留最终控制权。

示例：

- 原任务：整理房间
- 下一步：把地上的衣服放进洗衣篮

### 4. 正计时与自定义倒计时

用户可以针对当前任务选择：

- 正计时：从 0 开始记录实际投入时间。
- 倒计时：使用默认的 5、10、15 分钟或用户自定义的时长降低启动门槛。
- 默认进入正计时模式，倒计时模式需要主动切换。

倒计时最多保留 3 个快捷选项，用户可以在设置页修改，支持 1～180 分钟。

计时目标不是强迫用户完成任务，而是帮助用户先开始。系统按任务累计实际执行时间，暂停、再次开始和更换计时模式都不会丢失已经产生的记录。

### 5. 可选多层标签

标签功能默认关闭，避免在用户尚未需要分类时增加信息负载。

用户可以在个性化设置页开启多层标签，例如：

- 一级标签：学习
- 二级标签：AI、英语

任务可以关联多个标签。关闭标签功能后，任务标签和时间统计会从主流程中隐藏，但已有标签数据不会被删除。

### 6. 标签时间统计

开启标签功能后，系统按任务累计的执行时间生成标签统计。

- 子标签时间会汇总到父标签。
- 同一任务同时关联父标签和子标签时，父标签只计算一次时间，避免重复。
- 同一任务跨多个一级标签时会出现在各自的统计中。
- 标签统计只在标签功能开启时展示。

### 7. 个性化设置

个性化设置页用于控制可选能力和调整使用方式，包括：

- 开启或关闭标签功能。
- 开启或关闭“下一步建议”区域。
- 自定义倒计时选项，最多保留 3 个常用时长。
- 创建任意层级标签。
- 删除标签及其子标签。
- 查看标签时间统计。

关闭“下一步建议”后，系统隐藏主界面的建议区域，但保留任务已有的下一步内容，用户仍然可以手动编辑。

### 8. 任务循环模式

快速 Inbox 的每个任务卡提供“循环”按钮，点击后通过轻量弹窗设置：

- 不循环
- 每天
- 工作日
- 每周

循环模式会显示在任务卡上，并随任务保存。

- 每天：从计划日期开始，每天出现。
- 工作日：从计划日期开始，周一至周五出现。
- 每周：从计划日期开始，每周同一天出现。

当前版本用同一个任务记录展示循环日，尚未为每个循环日期生成独立实例。因此任务状态会作为该循环任务的当前状态展示；后续接入正式日历时再演进为独立 occurrence。

### 9. Inbox 标签快捷选择

标签功能开启后，任务卡显示“标签”按钮。弹窗内使用单选：选择某个标签会替换当前标签，也可以选择“不使用标签”清空。弹窗内还提供“添加标签”区域，可以新建顶层标签或子标签，新建后立即选中，不需要先进入设置页。

### 10. 任务状态记录

每个任务支持记录当前状态：

- 未开始
- 进行中
- 完成
- 暂时放下

状态记录用于反映任务的真实进展，避免只有“完成/未完成”两种过于粗糙的结果。

状态在任务卡和日历上用颜色区分，颜色只作辅助、文字始终保留：

- 进行中：绿色
- 未开始：紫色
- 暂时放下：黄色
- 完成：蓝色

在任务详情里把状态改为「完成」后，页面会返回今日 3 件事列表，方便马上看到今天的进度。

### 11. 今日状态与照顾清单

今日 3 件事页面顶部提供三个用户状态选项，用颜色区分并配有文字，避免只靠颜色传达信息：

- 绿色：满血
- 橙色：还不错
- 红色：低电量

状态按天记录，每次变化都会留下带时间戳的事件，而不是只覆盖当前值，为后续的状态曲线保留数据。右侧提供「照顾自己」入口，用金色胶囊按钮和状态选项区分开，点击进入 comfort 清单页。

comfort 清单由用户自己编写，也可以让系统生成候选、由用户确认后加入。每条包含标题、可选的具体做法和成本（轻 / 中 / 重）。当今日状态为低电量时，用户可以随机抽取一条轻量条目并采纳；采纳后该条目出现在今日列表里，用黄色与任务区分。

- 随机抽取优先选择成本最低的条目，并尽量避开最近用过的条目。
- 已采纳的条目在今日列表中只提供备注输入框和完成按钮，不显示加入/移除今日、循环和标签操作。
- 条目可以重复采纳，同一天同一条目只生成一次记录。
- 生成候选一次给出 3 条，低电量时只给低成本条目，已存在的条目不重复出现。
- 关闭「显示今日状态与照顾清单」后，状态选择和照顾入口隐藏，但记录不会被删除。

### 12. 完成庆祝音效

所有「完成」操作都会播放一段很短的庆祝音效：任务状态改为完成、照顾条目完成都会触发。

- 只在进入完成态时播放，重复点击、撤销后再次完成不会连续播放。
- 设置页提供开关和试听，关闭后所有完成操作都静默。
- 音效失败或被浏览器阻止时静默跳过，不影响完成流程。

## 任务卡交互

快速 Inbox 和今日 3 件事复用同一套任务卡样式。今日列表同样提供循环和标签操作。照顾清单使用独立的 comfort 卡片，不套用任务卡的操作区。

- 点击任务卡主体直接进入“开始一个小动作”详情页，不再单独提供“查看”按钮。
- 操作按钮最多使用一行三列，包括加入今日/移出今日、循环和标签。
- 标签功能开启后，标签按钮直接显示已选标签名称。
- 标签弹窗和循环设置弹窗使用轻量底部弹层，避免离开当前列表。

## 今日日历

今日 3 件事页面顶部提供日历入口。

- 默认展示当前日期。
- 点击日期可以查看当天安排的任务。
- 当天任务显示标题、下一步和具体状态：未开始、进行中、完成或暂时放下。
- 日历会为有任务的日期显示数量标记。
- 任务通过 `plannedDate` 保留所属日期；跨天时旧的今日标记自动收起，历史日期仍可查询。

## 导航与页面结构

主产品使用底部 Tab 导航，包含三个一级入口：

1. **快速 Inbox**：输入、收集和整理任务。
2. **今日 3 件事**：查看今天想优先推进的任务。
3. **设置**：管理可选功能、倒计时选项和标签。

“开始一个小动作”是任务详情页，不占据底部 Tab。用户从 Inbox 或今日列表点击任务卡主体进入，通过返回按钮回到原列表。

快速 Inbox 和今日 3 件事在任务数量较多时使用内部滚动列表，避免整个页面被长列表撑开。移动端优先保持单页面、少层级，避免一次展示过多信息。

## MVP 暂不包含

- 复杂日历和排期系统
- 项目管理功能
- 长期习惯打卡系统
- 社交监督或协作功能
- 成就系统
- 情绪复盘、自我觉察或心理建议
- 数据统计仪表盘

以上能力未纳入当前 MVP，但不代表永久排除。后续如需引入，一律作为可关闭模块实现，并遵守「功能扩展与开关约定」：

- 复杂日历、项目管理、社交协作等带来大量决策的功能，需要先证明它们能减少而不是增加用户的切换成本。
- 长期习惯打卡和成就系统要先解决“不制造压力”的问题，否则不做。
- “情绪复盘、自我觉察或心理建议”的边界是：只做用户主动的每日状态记录与自助行动建议，不做情绪分析、诊断或心理建议。

## 设计原则

- 少输入：用户能少写就少写，能点选就点选。
- 少决策：默认限制范围，避免让用户面对过多选择。
- 可重新开始：暂停、搁置和重新开始都应该是正常路径。
- 先开始再完善：产品重点不是规划得完美，而是帮助用户启动。
- 不制造压力：避免惩罚式提醒、断签焦虑和过度自我管理。

## 功能扩展与开关约定

产品长期目标是减少用户在多个工具之间来回切换的成本，因此功能会持续增加。功能数量本身不等于臃肿，真正的风险是默认决策量、模块之间的耦合度，以及开关组合带来的不可预测行为。以下约定用于判断一个新能力是否可以进入产品，以及进入后如何存在。

### 衡量标准

不追求“功能越少越好”，而是守住三个指标：

- 首屏决策量：打开 App 后，用户需要立刻做多少个选择。
- 开关组合数：A 开 B 关时，行为是否仍然正确、可预期。
- 耦合度：关闭一个模块，是否会悄悄影响其他模块的数据或统计。

### 模块分层

按与核心问题的距离分成三层，越靠外圈，准入越严格：

- 内核（不可关闭）：快速 Inbox、今日 3 件事、把任务拆成一个可以马上开始的下一步。
- 内圈（可选，默认极简）：围绕“降低启动成本”的能力，例如计时器、循环、下一步建议、用户状态与 comfort。
- 外圈（可选，默认关闭）：组织、时间与回顾能力，例如多层标签、日历、状态曲线、统计。

### 开关的三种语义

设置页中的“关闭”必须明确属于哪一种，不允许含糊：

| 语义 | 关闭后的行为 | 数据 |
| --- | --- | --- |
| 隐藏界面 | 入口和展示消失，不影响其他模块 | 保留 |
| 关闭行为 | 不再触发副作用，例如关闭庆祝音效 | 保留 |
| 退出派生计算 | 不再进入统计、曲线等派生结果 | 保留 |

统一约定：数据永远保留，开关只影响“是否展示”和“是否参与派生计算”，不影响核心流程，也不删除已有数据。

### 准入规则

1. 内核功能永不提供开关；其余模块一律可以被关闭。
2. 新功能默认关闭或默认极简，不做“默认打开”的功能加法。
3. 开关必须正交：关闭 A 不得改变 B 的展示、数据或计算。
4. 模块只写自己的数据、只读核心数据，禁止跨模块写入。
5. 派生数据（曲线、统计）一律从事件记录计算，不落库冗余状态。
6. 首屏预算固定：今日页只保留有限入口，增加一个入口就要替换或收起一个。
7. 每个新模块必须回答：它替代了用户现在用的哪个工具？没有答案时先不做。
8. 设置页按“核心 / 可选 / 实验”分组，关闭入口必须容易发现。

### 对自我记录类功能的额外约束

涉及状态、情绪或自我观察的功能，必须遵守“不制造压力”的设计原则：

- 不做连续打卡、断签提醒和好坏评分。
- 自述内容（例如选择状态的原因）永远可选、可跳过，并且不阻塞主流程。
- 这类提示默认关闭，由用户主动开启。
- 不生成心理建议或诊断，只帮助用户看到自己的规律。

### 后续纳入范围

以下能力暂不进入当前版本，但不排除后续以内圈或外圈模块引入，并且必须遵守上述约定：

- 每日用户状态记录与状态曲线。
- comfort / 自我照顾清单与 AI 生成建议。
- 照片等富媒体记录。
- 情绪复盘、自我觉察相关功能。

## MVP 成功标准

第一版产品是否成功，主要看它能否帮助用户更容易做到以下事情：

- 快速记录脑子里的任务。
- 从一堆任务中选出今天想优先推进的事项，同时不被硬性数量限制阻断。
- 把模糊任务变成可以执行的下一步。
- 通过正计时或短时间倒计时降低启动门槛，并看到自己在每个任务上的实际投入。
- 用状态记录保留任务进展，而不是因为没完成就丢失上下文。
- 在需要时选择开启多层标签，并按标签查看时间分布，而不被迫一开始就分类。
