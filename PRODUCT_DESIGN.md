# ADHDer MVP 产品设计文档

## 产品方向

ADHDer 是一款帮助 ADHD 用户降低生活管理压力的轻量工具。MVP 阶段不追求完整的效率系统，而是优先解决一个核心问题：用户脑子里有很多事情，但很难整理、选择并开始。

第一版产品的重点是帮助用户完成从“想到一件事”到“开始做一个很小的动作”的过程。

最终产品形态优先考虑移动 App 或微信小程序。Web 版本采用移动端优先设计，交互结构尽量贴近 App，便于后续迁移到 App 或小程序。

## 产品目标

帮助 ADHDer 把脑子里的混乱事项快速收集起来，选出今天最重要的少数任务，并把它们转化成可以立刻开始的小动作。

## 核心用户问题

- 脑子里同时装着很多任务，容易混乱和过载。
- 待办事项太多时，不知道今天应该先做什么。
- 任务描述太大或太模糊，导致迟迟无法开始。
- 已经知道要做什么，但启动成本很高。
- 任务状态不是简单的完成或未完成，中间经常会暂停、转移或搁置。

## MVP 功能范围

### 1. 快速 Inbox

用户可以随手记录脑子里的任务、想法和待办事项，不需要立即分类、排序或安排时间。

目标是降低输入压力，让用户先把事情从脑子里倒出来。

### 2. 今日 3 件事

“3 件事”作为建议和注意力提示，而不是强制限制。

建议用户先选择 1～3 件今天推进；如果用户想继续添加，也可以根据自己的状态加入更多任务。

默认展示建议数量的作用是减少决策压力，但产品不阻止用户管理自己的今日重点。

### 3. 任务拆成下一步

系统辅助用户把一个模糊任务拆成一个更小、更容易开始的下一步动作。

用户可以接受、编辑或重新生成系统建议。系统负责降低拆解成本，用户保留最终控制权。

示例：

- 原任务：整理房间
- 下一步：把地上的衣服放进洗衣篮

### 4. 正计时与自定义倒计时

用户可以针对当前任务选择：

- 正计时：从 0 开始记录实际投入时间。
- 倒计时：使用默认的 5、10、15 分钟或用户自定义的时长降低启动门槛。
- 默认进入正计时模式，倒计时模式需要主动切换。

倒计时最多保留 3 个快捷选项，用户可以在设置页修改，支持 1～180 分钟。

计时目标不是强迫用户完成任务，而是帮助用户先开始。系统按任务累计实际执行时间，暂停、再次开始和更换计时模式都不会丢失已经产生的记录。

### 5. 可选多层标签

标签功能默认关闭，避免在用户尚未需要分类时增加信息负载。

用户可以在个性化设置页开启多层标签，例如：

- 一级标签：学习
- 二级标签：AI、英语

任务可以关联多个标签。关闭标签功能后，任务标签和时间统计会从主流程中隐藏，但已有标签数据不会被删除。

### 6. 标签时间统计

开启标签功能后，系统按任务累计的执行时间生成标签统计。

- 子标签时间会汇总到父标签。
- 同一任务同时关联父标签和子标签时，父标签只计算一次时间，避免重复。
- 同一任务跨多个一级标签时会出现在各自的统计中。
- 标签统计只在标签功能开启时展示。

### 7. 个性化设置

个性化设置页用于控制可选能力和调整使用方式，包括：

- 开启或关闭标签功能。
- 开启或关闭“下一步建议”区域。
- 自定义倒计时选项，最多保留 3 个常用时长。
- 创建任意层级标签。
- 删除标签及其子标签。
- 查看标签时间统计。

关闭“下一步建议”后，系统隐藏主界面的建议区域，但保留任务已有的下一步内容，用户仍然可以手动编辑。

### 8. 任务循环模式

快速 Inbox 的每个任务卡提供“循环”按钮，点击后通过轻量弹窗设置：

- 不循环
- 每天
- 工作日
- 每周

循环模式会显示在任务卡上，并随任务保存。

- 每天：从计划日期开始，每天出现。
- 工作日：从计划日期开始，周一至周五出现。
- 每周：从计划日期开始，每周同一天出现。

当前版本用同一个任务记录展示循环日，尚未为每个循环日期生成独立实例。因此任务状态会作为该循环任务的当前状态展示；后续接入正式日历时再演进为独立 occurrence。

### 9. Inbox 标签快捷选择

标签功能开启后，快速 Inbox 的任务卡显示“标签”按钮。用户可以直接在弹窗中为任务添加或移除多个标签，不需要先进入任务详情页。

### 10. 任务状态记录

每个任务支持记录当前状态：

- 未开始
- 进行中
- 完成
- 暂时放下

状态记录用于反映任务的真实进展，避免只有“完成/未完成”两种过于粗糙的结果。

## 任务卡交互

快速 Inbox 和今日 3 件事复用同一套任务卡样式。今日列表同样提供循环和标签操作。

- 点击任务卡主体直接进入“开始一个小动作”详情页，不再单独提供“查看”按钮。
- 操作按钮最多使用一行三列，包括加入今日/移出今日、循环和标签。
- 标签功能开启后，标签按钮直接显示已选标签名称；多标签时显示首个标签和剩余数量。
- 标签弹窗和循环设置弹窗使用轻量底部弹层，避免离开当前列表。

## 今日日历

今日 3 件事页面顶部提供日历入口。

- 默认展示当前日期。
- 点击日期可以查看当天安排的任务。
- 当天任务显示标题、下一步和具体状态：未开始、进行中、完成或暂时放下。
- 日历会为有任务的日期显示数量标记。
- 任务通过 `plannedDate` 保留所属日期；跨天时旧的今日标记自动收起，历史日期仍可查询。

## 导航与页面结构

主产品使用底部 Tab 导航，包含三个一级入口：

1. **快速 Inbox**：输入、收集和整理任务。
2. **今日 3 件事**：查看今天想优先推进的任务。
3. **设置**：管理可选功能、倒计时选项和标签。

“开始一个小动作”是任务详情页，不占据底部 Tab。用户从 Inbox 或今日列表点击任务卡主体进入，通过返回按钮回到原列表。

快速 Inbox 和今日 3 件事在任务数量较多时使用内部滚动列表，避免整个页面被长列表撑开。移动端优先保持单页面、少层级，避免一次展示过多信息。

## MVP 暂不包含

- 复杂日历和排期系统
- 项目管理功能
- 长期习惯打卡系统
- 社交监督或协作功能
- 成就系统
- 情绪复盘、自我觉察或心理建议
- 数据统计仪表盘

这些功能可以在后续版本中根据用户反馈逐步评估。

## 设计原则

- 少输入：用户能少写就少写，能点选就点选。
- 少决策：默认限制范围，避免让用户面对过多选择。
- 可重新开始：暂停、搁置和重新开始都应该是正常路径。
- 先开始再完善：产品重点不是规划得完美，而是帮助用户启动。
- 不制造压力：避免惩罚式提醒、断签焦虑和过度自我管理。

## MVP 成功标准

第一版产品是否成功，主要看它能否帮助用户更容易做到以下事情：

- 快速记录脑子里的任务。
- 从一堆任务中选出今天想优先推进的事项，同时不被硬性数量限制阻断。
- 把模糊任务变成可以执行的下一步。
- 通过正计时或短时间倒计时降低启动门槛，并看到自己在每个任务上的实际投入。
- 用状态记录保留任务进展，而不是因为没完成就丢失上下文。
- 在需要时选择开启多层标签，并按标签查看时间分布，而不被迫一开始就分类。
